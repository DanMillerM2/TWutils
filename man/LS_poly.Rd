% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{LS_poly}
\alias{LS_poly}
\title{Characterize mapped landslide polygons}
\usage{
LS_poly(
  DEM = NOFILE,
  polyFile = NOFILE,
  polyID = NOFILE,
  inGrad = NOFILE,
  inTan = NOFILE,
  inProf = NOFILE,
  inFoS = NOFILE,
  outGrad = NOFILE,
  gradRadius = -9999,
  outTan = NOFILE,
  tanRadius = -9999,
  outProf = NOFILE,
  profRadius = -9999,
  outNodes = NOFILE,
  outCsv = NOFILE,
  outInit = NOFILE,
  scratchDir = NOFILE,
  executableDir = NOFILE
)
}
\arguments{
\item{DEM}{Character: input DEM (.flt or .tif), full path.}

\item{polyFile}{Character: input landslide polygon shapefile.}

\item{polyID}{Character: name of the ID field for the input polygons.}

\item{inGrad, inTan, inProf}{Character: precomputed gradient, tangential
curvature and profile curvature rasters (each optional).}

\item{inFoS}{Character: input factor-of-safety raster.}

\item{outGrad, outTan, outProf}{Character: rasters for LS_poly to compute and
write (each optional).}

\item{gradRadius, tanRadius, profRadius}{Numeric: radii in meters used when
computing the corresponding output raster.}

\item{outNodes}{Character: output node point shapefile (.shp).}

\item{outCsv}{Character: output csv of patch statistics.}

\item{outInit}{Character: output initiation zone raster.}

\item{scratchDir}{Character: scratch directory.}

\item{executableDir}{Character: directory holding LS_poly.exe.}
}
\value{
0 on success. Stops with a message on any failure.
}
\description{
A wrapper for Fortran program LS_poly. LS_poly reads a polygon shapefile of
landslide scars, generates centerlines through each polygon, and builds a
linked-node list along them. A likely initiation zone is delineated from
the upslope end of each polygon, extending downslope a distance equal to
the average polygon width. Statistics are calculated for those zones,
including gradient, tangential curvature, profile curvature and factor of
safety.
}
\details{
Gradient and curvature rasters may either be read from disk (the \verb{in*}
arguments) or calculated by LS_poly, in which case a radius must be given
and the result can be written out (the \verb{out*} arguments) for reuse. A
factor-of-safety raster must always be supplied; generate it with the FoS
program.
}
