% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_file_utilities.R
\name{raster_list_entry}
\alias{raster_list_entry}
\title{Describe one entry in a raster list}
\usage{
raster_list_entry(
  file,
  statistic = "MEAN",
  ...,
  output_field_name = NULL,
  flags = character(0)
)
}
\arguments{
\item{file}{Raster to sample (full path).}

\item{statistic}{Bare-flag statistic, e.g. "MEAN". A character vector writes
several.}

\item{...}{Any further named arguments the program accepts, written as
\code{NAME = value}.}

\item{output_field_name}{Field name for the result, written as the
OUTPUT FIELD NAME argument.}

\item{flags}{Character vector of further bare flags, e.g. "DO NODES".}
}
\value{
A raster-list object for \code{\link[=write_raster_list]{write_raster_list()}}.
}
\description{
Becomes one FILE line inside the RASTER LIST block: a raster to sample along
the channel network, the statistic used to reduce it, and the field the
result is written into.
}
\examples{
\dontrun{
raster_list_entry("d:/data/pfa/nehalem/grad1_30",
                  statistic = "MEAN",
                  output_field_name = "meanGrad",
                  flags = "DO NODES")
}
}
