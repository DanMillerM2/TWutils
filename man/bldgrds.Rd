% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{bldgrds}
\alias{bldgrds}
\title{--- bldgrds, Build the channel network from a DEM ---}
\usage{
bldgrds(dem = NOFILE, scratch_dir = NOFILE, ..., executable_dir = NULL)
}
\arguments{
\item{dem}{Character: file name (full path) of the DEM.}

\item{scratch_dir}{Character: scratch directory. The bldgrds input file is
written here.}

\item{...}{Further arguments passed to \code{\link[=bldgrds_input]{bldgrds_input()}}.}

\item{executable_dir}{Character: directory holding bldgrds.exe. There is
no default location: it must always be supplied.}
}
\value{
0 on success. Stops with a message on any failure.
}
\description{
A wrapper for Fortran program bldgrds. bldgrds computes flow direction and
D-infinity contributing area for a DEM, then traces the channel network
downstream and writes it out as a node-list database -- consumed by
downstream programs such as netrace -- plus, optionally, a node point
shapefile. Requesting that shapefile (via \code{node_shapefile}, a
\code{\link[=bldgrds_input]{bldgrds_input()}} argument) requires an \verb{ATTRIBUTE LIST} block in the
input file; \code{\link[=bldgrds_input]{bldgrds_input()}} supplies one automatically -- see its
\code{attribute_list} argument and \code{\link[=bldgrds_default_attributes]{bldgrds_default_attributes()}}.
}
\details{
Unlike \code{\link[=elev_deriv]{elev_deriv()}}, \code{\link[=contributing_area]{contributing_area()}} and \code{\link[=bldgrds_nochannels]{bldgrds_nochannels()}}
above, there is no single output raster to read back afterwards: bldgrds'
products are the node-list database and whatever optional rasters/
shapefiles were requested via \code{\link[=bldgrds_input]{bldgrds_input()}}. This wrapper therefore
only supports build-and-run mode, the same as \code{\link[=RIL]{RIL()}}, \code{\link[=huntLS]{huntLS()}},
\code{\link[=LShunter]{LShunter()}}, \code{\link[=LS_poly]{LS_poly()}} and \code{\link[=samplePoints]{samplePoints()}}: there is no existing-
input-file mode and no read-only mode.

Every tuning parameter beyond the two named here (channel-initiation
thresholds, D8 weighting, the road-crossing-excavation and water-mask
options, the node point shapefile request, and the attribute list) is
passed straight through to \code{\link[=bldgrds_input]{bldgrds_input()}}, which documents them.
}
\seealso{
\code{\link[=bldgrds_input]{bldgrds_input()}}, \code{\link[=bldgrds_default_attributes]{bldgrds_default_attributes()}},
\code{\link[=bldgrds_nochannels]{bldgrds_nochannels()}}
}
