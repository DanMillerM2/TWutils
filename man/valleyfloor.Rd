% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{valleyfloor}
\alias{valleyfloor}
\title{Height above channel, valley width, and (optionally) flood inundation}
\usage{
valleyfloor(dem = NOFILE, scratch_dir = NOFILE, ..., executable_dir = NULL)
}
\arguments{
\item{dem}{Character: input DEM (full path).}

\item{scratch_dir}{Character: scratch directory. The ValleyFloor input
file is written here (the \code{.dat} output file is not -- see above).}

\item{...}{Further arguments passed to \code{\link[=valleyfloor_input]{valleyfloor_input()}}: channel
selection, valley-buffer/height-above/valley-width parameters, the
optional output rasters, and \code{attribute_list}.}

\item{executable_dir}{Character: directory holding ValleyFloor.exe. There
is no default location: it must always be supplied.}
}
\value{
The full path of the \verb{valleyfloor_<ID>.dat} binary data file
ValleyFloor wrote (or updated, if \code{read_data = TRUE}).
}
\description{
A wrapper for Fortran program ValleyFloor. For each selected channel,
ValleyFloor builds a cell-by-cell height/depth-above-channel surface
across the surrounding valley, then, when requested, measures valley
width at each of a series of depth-above-channel thresholds and/or (with
\code{method = 4}) fits a TIN-based flood-inundation surface.
}
\details{
ValleyFloor's actual product is a binary per-channel data file,
\verb{valleyfloor_<ID>.dat}, written next to the DEM (not in \code{scratch_dir}) --
see \code{\link[=valleyfloor_dat_file]{valleyfloor_dat_file()}} for how \verb{<ID>} is resolved. This file is
always written, whatever else was requested; every output raster
\code{\link[=valleyfloor_input]{valleyfloor_input()}} knows about (\code{out_elev}, \code{out_depth},
\code{out_flood_height}, \code{out_flood_depth}, \code{out_d8}, ...) is optional, and the
program runs perfectly well with none of them requested. Ask for whichever
rasters you want via \code{...} and read them back yourself afterward (with
\code{\link[terra:rast]{terra::rast()}}, say) using the paths you supplied.
}
\seealso{
\code{\link[=valleyfloor_input]{valleyfloor_input()}}, \code{\link[=valleyfloor_dat_file]{valleyfloor_dat_file()}},
\code{\link[=valleyfloor_default_attributes]{valleyfloor_default_attributes()}}
}
