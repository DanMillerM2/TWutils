% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{RIL}
\alias{RIL}
\title{--- RIL, Delineate a channel network and its riparian landforms ---}
\usage{
RIL(
  dem = NOFILE,
  scratch_dir = NOFILE,
  out_RIL = NOFILE,
  ...,
  executable_dir = NULL
)
}
\arguments{
\item{dem}{Character: input DEM (full path).}

\item{scratch_dir}{Character: scratch directory. The RIL input file is
written here.}

\item{out_RIL}{Character: output RIL raster (.flt).}

\item{...}{Further arguments passed to \code{\link[=RIL_input]{RIL_input()}}: thresholds, the
optional input and output rasters, the road shapefile, and
\code{attribute_list}.}

\item{executable_dir}{Character: directory holding RIL.exe. Defaults to
get_executable_path().}
}
\value{
A \code{SpatRaster} of the output RIL classes.
}
\description{
A wrapper for Fortran program RIL. RIL finds channel initiation points on a
DEM, traces the channel network downstream, and classifies the terrain
around it into valley floor, hollow and inner-gorge landforms. It writes a
raster of those classes and, optionally, a node point shapefile carrying a
list of per-node attributes.
}
\details{
Every tuning parameter is passed straight through to \code{\link[=RIL_input]{RIL_input()}}, which
documents them and supplies defaults from the Post Mortem reference run. A
minimal call therefore needs only a DEM, a scratch directory and an output
raster name.

Existence of the optional input rasters and the road shapefile is checked
inside \code{\link[=RIL_input]{RIL_input()}}, which names the offending argument if one is missing.
}
\seealso{
\code{\link[=RIL_input]{RIL_input()}}, \code{\link[=ril_attribute]{ril_attribute()}}, \code{\link[=ril_term]{ril_term()}},
\code{\link[=ril_default_attributes]{ril_default_attributes()}}
}
