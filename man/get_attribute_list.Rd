% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_file_utilities.R
\name{get_attribute_list}
\alias{get_attribute_list}
\title{Parse an ATTRIBUTE LIST block out of an input file}
\usage{
get_attribute_list(
  infile,
  start_keyword = "ATTRIBUTE LIST",
  end_pattern = "^END( ATTRIBUTE)? LIST$"
)
}
\arguments{
\item{infile}{A tibble created by \code{\link[=get_input_file]{get_input_file()}}.}

\item{start_keyword}{Keyword opening the block.}

\item{end_pattern}{Regular expression matching the keyword that closes it.
The default accepts both the RIL and netrace terminators.}
}
\value{
A list of \code{\link[=attribute_spec]{attribute_spec()}} objects, empty if the file has no
attribute list.
}
\description{
Returns the same \code{\link[=attribute_spec]{attribute_spec()}} objects the writers consume, so a
working input file can be read in, edited in R, and written back out.
}
\details{
Recognized arguments (FILE, OUTPUT FIELD, UNITS, TYPE, LENGTH, and the
REPLACE flag) are mapped onto the corresponding fields. Anything else is
kept in \code{extra} and re-emitted verbatim, so a block using arguments this
file has never seen still round-trips.
}
