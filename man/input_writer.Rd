% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_file_utilities.R
\name{input_writer}
\alias{input_writer}
\title{Open an input file and return a set of functions for writing to it}
\usage{
input_writer(program, scratch_dir, file_name, overwrite = TRUE, note = NULL)
}
\arguments{
\item{program}{Program name, written into the header comment.}

\item{scratch_dir}{Scratch directory. The input file is written here, and
the directory must already exist.}

\item{file_name}{Base name of the input file.}

\item{overwrite}{If FALSE, stop rather than overwrite an existing input file.}

\item{note}{Optional extra comment line for the header.}
}
\value{
A list of writer functions; see Details.
}
\description{
This is the one place in the file that knows how a keyword line is laid
out. Every writer in Section 5 opens a file with this function and then
does nothing but name keywords and hand over values.
}
\details{
Input_writer checks that the scratch directory exists,
builds the output path, defines a byte-for-byte identical \code{write_input()}
closure, and emits an identical three-line header comment.

The returned list holds three writer functions and the output path:

\describe{
\item{\code{keyword(name, ...)}}{Writes one keyword line. Arguments in \code{...}
that are named are emitted as \code{NAME = value}; unnamed arguments are
emitted as bare values. All of them are joined with ", ". Called with
no arguments at all, it writes a bare \verb{NAME:} flag line. A \code{.indent}
argument adds leading spaces, for the nested blocks that a few programs
use.}
\item{\code{optional(name, value, ...)}}{The same, but writes nothing at all
when \code{value} is absent, as judged by \code{is_missing_path()}. Returns TRUE
if a line was written. Because R evaluates arguments lazily, the
remaining \code{...} arguments are never evaluated when the line is skipped;
that is what lets a call like
\code{optional("ROAD SHAPEFILE", roads, BUFFER = road_buffer)} be safe even
when \code{road_buffer} was never supplied.}
\item{\code{line(...)}}{Writes an arbitrary line, pasted together with no
separator. The escape hatch for the handful of genuinely irregular
blocks.}
\item{\code{file_path}}{The full path of the file being written.}
}
}
