% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_file_utilities.R
\name{bldgrds_enforce_input}
\alias{bldgrds_enforce_input}
\title{Create an input file for Fortran program bldgrds, enforcing an existing channel network}
\usage{
bldgrds_enforce_input(
  dem,
  scratch_dir,
  channel_mask,
  channel_mask_dig,
  channel_mask_radius = 0,
  channel_mask_directional = FALSE,
  channel_mask_init_all = FALSE,
  node_shapefile = NOFILE,
  node_splits = NULL,
  drainage_wing_raster = NOFILE,
  hand_raster = NOFILE,
  hand_flow_threshold = NULL,
  hand_normalize = FALSE,
  twi_raster = NOFILE,
  twi_gradient_length_scale = NULL,
  attribute_list = bldgrds_default_attributes(),
  overwrite = TRUE
)
}
\arguments{
\item{dem}{Input DEM (full path).}

\item{scratch_dir}{Scratch directory; the input file is written here.}

\item{channel_mask}{Existing channel-network polyline shapefile to enforce (\verb{CHANNEL MASK: FILE}).}

\item{channel_mask_dig}{Depth (DEM elevation units) to excavate/burn \code{channel_mask} into the
DEM (\verb{CHANNEL MASK: DIG}).}

\item{channel_mask_radius}{Radius used when excavating \code{channel_mask} into the DEM (\verb{CHANNEL MASK: RADIUS}).}

\item{channel_mask_directional}{If TRUE, treat \code{channel_mask} as directional (\verb{CHANNEL MASK: DIRECTIONAL}).}

\item{channel_mask_init_all}{If TRUE, seed channel initiation at every \code{channel_mask} cell,
not just its ends (\verb{CHANNEL MASK: INIT ALL}).}

\item{node_shapefile}{Optional: output node point shapefile (\verb{OUTPUT NODE POINT SHAPEFILE}).}

\item{node_splits}{Optional: number of pieces to split each channel segment into for
\code{node_shapefile}. Only meaningful with \code{node_shapefile}.}

\item{drainage_wing_raster}{Optional: output drainage wing raster (\verb{DRAINAGE WING RASTER}).}

\item{hand_raster}{Optional: output HAND (height above nearest drainage) raster (\verb{HAND RASTER}).}

\item{hand_flow_threshold}{Flow-accumulation threshold (as a proportion) for \code{hand_raster}
(\verb{HAND RASTER: FLOW THRESHOLD}). Only meaningful with \code{hand_raster}.}

\item{hand_normalize}{If TRUE, normalize \code{hand_raster} (\verb{HAND RASTER: NORMALIZE}). Only
meaningful with \code{hand_raster}.}

\item{twi_raster}{Optional: output topographic wetness index raster (\verb{TWI RASTER}).}

\item{twi_gradient_length_scale}{Length in meters over which gradient is measured for
\code{twi_raster} (\verb{TWI RASTER: GRADIENT LENGTH SCALE}). Only meaningful with \code{twi_raster}.}

\item{attribute_list}{List of \code{\link[=attribute_spec]{attribute_spec()}} objects, written as an \verb{ATTRIBUTE LIST}
block after every other keyword (same ordering constraint as \code{\link[=bldgrds_input]{bldgrds_input()}} -- must come
last). Defaults to \code{\link[=bldgrds_default_attributes]{bldgrds_default_attributes()}}. \strong{A node point shapefile cannot be
created without an \verb{ATTRIBUTE LIST} block}, so passing \code{attribute_list = list()} together
with a \code{node_shapefile} is an error here rather than a silently broken run.}

\item{overwrite}{If TRUE, allow overwriting an existing input file.}
}
\value{
The input file path, invisibly.
}
\description{
bldgrds normally initiates new channels from area-slope/plan-curvature/local-relief
thresholds (see \code{\link[=bldgrds_input]{bldgrds_input()}}). This builder instead traces the channel network from an
existing, previously-mapped channel-network polyline shapefile (\code{channel_mask}), excavating
("digging") it into the DEM, and precludes any new channel initiation outside that mask --
\verb{NO NEW CHANNELS} is always written. None of \code{\link[=bldgrds_input]{bldgrds_input()}}'s channel-initiation-criteria
arguments (aspect/plan-curvature/D8 length scales, initiation buffer, area-slope/plan-
curvature/local-relief thresholds, minimum flow length, ...) are needed or accepted here,
since no new initiation happens.
}
\details{
Reproduces the keyword grammar of a working reference run (Skykomish project): \verb{DEM FILE},
\code{SCRATCH}, \verb{CHANNEL MASK} (w/ \code{FILE}, \code{DIG}, \code{RADIUS}, \code{DIRECTIONAL}, \verb{INIT ALL}),
\verb{NO NEW CHANNELS}, \verb{OUTPUT NODE POINT SHAPEFILE} (w/ \code{SPLITS}), \verb{DRAINAGE WING RASTER},
\verb{HAND RASTER} (w/ \verb{FLOW THRESHOLD}, \code{NORMALIZE}), \verb{TWI RASTER} (w/ \verb{GRADIENT LENGTH SCALE}),
and an \verb{ATTRIBUTE LIST} block -- closed with \verb{END LIST}, NOT \verb{END ATTRIBUTE LIST} like
\code{\link[=bldgrds_input]{bldgrds_input()}} writes. That's a real discrepancy between the two reference files this
package's \code{bldgrds_input()}/\code{bldgrds_enforce_input()} are each built from, not a typo here --
see the "bldgrds" entries in this package's \code{CLAUDE.md}; the two haven't been reconciled.
}
