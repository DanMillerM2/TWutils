% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_file_utilities.R
\name{valleyfloor_dat_file}
\alias{valleyfloor_dat_file}
\title{Locate ValleyFloor's binary output data file}
\usage{
valleyfloor_dat_file(dem, data_id = NOFILE)
}
\arguments{
\item{dem}{Input DEM (full path), exactly as passed to
\code{\link[=valleyfloor_input]{valleyfloor_input()}}/\code{\link[=valleyfloor]{valleyfloor()}}.}

\item{data_id}{Optional: an explicit data ID, matching \code{data_id} as
actually passed to \code{\link[=valleyfloor_input]{valleyfloor_input()}}/\code{\link[=valleyfloor]{valleyfloor()}}. Omit (or pass
\code{NOFILE}) to derive the ID from \code{dem} the same way ValleyFloor itself
does when no \verb{DATA ID} keyword is written.}
}
\value{
The full path of the \verb{valleyfloor_<ID>.dat} file.
}
\description{
ValleyFloor writes its per-channel results (distance/height/depth above
channel, valley width, node attributes) to a binary data file named
\verb{valleyfloor_<ID>.dat}, in the \emph{DEM's own directory} -- \code{ValleyFloor.f90}
builds this path as \verb{TRIM(refDEM\%path)//'valleyfloor_'//TRIM(dataID)// '.dat'}, not anywhere under \code{scratch_dir}. The file is opened
unconditionally on every run (for reading, if \code{read_data = TRUE}; for
writing/replacing otherwise), regardless of which, if any, output rasters
were requested -- it is ValleyFloor's real output, and the only one
that's never optional.
}
\details{
\verb{<ID>} is \code{data_id} if one was supplied (matching an explicit
\verb{DATA ID} keyword -- see \code{\link[=valleyfloor_input]{valleyfloor_input()}}'s \code{data_id} argument, only
honored in \code{all_channels = TRUE} mode); otherwise it falls back to the ID
\code{DEM_module}'s \code{resolveDEMname()} derives from the DEM's own file name:
everything after the first underscore in its (extensionless) base name,
e.g. \code{elev_Cherry} -> \code{Cherry}, or the whole base name if it has no
underscore.
}
\seealso{
\code{\link[=valleyfloor_input]{valleyfloor_input()}}, \code{\link[=valleyfloor]{valleyfloor()}}
}
