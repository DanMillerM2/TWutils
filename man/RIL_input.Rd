% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_file_utilities.R
\name{RIL_input}
\alias{RIL_input}
\title{--- RIL_input, Create an input file for Fortran program RIL ---}
\usage{
RIL_input(
  dem,
  scratch_dir,
  out_RIL,
  radius = 7.5,
  closest_node = c(`NUM WIDTHS` = 0, `MAX RADIUS` = 250, `MIN RADIUS` = 250),
  as2_threshold = 1000,
  plan_curvature_threshold = 0.005,
  gradient_threshold = 0.5,
  fluvial_area_threshold = 0.07,
  valley_depth_max = 5,
  valley_buffer = 2.5,
  valley_max_hole = 1000,
  valley_min_patch = 20,
  hollow_gradient = c(PRIMARY = 0.3, SECONDARY = 0.25, DIF1 = 1, DIF2 = 0.45,
    `MIN POLY GRAD` = 0.2),
  hollow_tangential = c(PRIMARY = -0.005, SECONDARY = -0.02, `MIN POLY TAN` = 0),
  hollow_profile = c(PRIMARY = -0.01, SECONDARY = -0.06, `MIN POLY PROF` = -0.015),
  grad_proportion = c(GRAD = 0.5, PROP = 0.1),
  tan_proportion = c(TAN = 0, PROP = 0.5),
  fill_hollow_embayments = TRUE,
  hollow_area_threshold = 5,
  hollow_max_hole = 1000,
  hollow_min_patch = c(INITIAL = 0, FINAL = 300),
  gorge_gradient = c(PRIMARY = 0.7, SECONDARY = 0.45, DIF1 = 0.95, DIF2 = 0.7),
  gorge_max_hole = 1000,
  gorge_min_patch = 0,
  slope_thresholds = c(SLOPE = 0.4, STEEP = 0.7),
  curve_thresholds = c(CONVERGENT = 0.01, DIVERGENT = -0.01),
  edge_smoothing_iterations = 11,
  road_shapefile = NOFILE,
  road_buffer = 15,
  in_closest_node = NOFILE,
  in_dist_to_channel = NOFILE,
  in_drainage_wing = NOFILE,
  in_valley_floor = NOFILE,
  in_upgrad = NOFILE,
  in_downgrad = NOFILE,
  in_grad = NOFILE,
  in_tangential = NOFILE,
  in_plan = NOFILE,
  in_prof = NOFILE,
  out_closest_node = NOFILE,
  out_dist_to_channel = NOFILE,
  out_drainage_wing = NOFILE,
  out_valley_floor = NOFILE,
  out_upgrad = NOFILE,
  out_downgrad = NOFILE,
  out_grad = NOFILE,
  out_tangential = NOFILE,
  out_plan = NOFILE,
  out_prof = NOFILE,
  out_nodes = NOFILE,
  out_zero_order = NOFILE,
  attribute_list =
    ril_default_attributes("c:\\\\work\\\\data\\\\postmortem\\\\prism_wasp_m"),
  use_ltd = TRUE,
  debug = FALSE,
  overwrite = TRUE
)
}
\arguments{
\item{dem}{Input DEM (full path).}

\item{scratch_dir}{Scratch directory; the input file is written here.}

\item{out_RIL}{Output RIL raster (.flt). The one required output.}

\item{radius}{Radius in meters for calculating elevation derivatives.}

\item{closest_node}{Named vector of the CLOSEST NODE RASTER search
parameters: NUM WIDTHS, MAX RADIUS, MIN RADIUS.}

\item{as2_threshold}{AS2 threshold in square meters for channel initiation.}

\item{plan_curvature_threshold}{Plan curvature threshold for initiation.}

\item{gradient_threshold}{Gradient threshold for initiation, based on a
centered-window gradient.}

\item{fluvial_area_threshold}{Contributing area in square kilometers above
which a channel is treated as fluvial.}

\item{valley_depth_max}{Maximum channel depth in meters for delineating the
valley floor.}

\item{valley_buffer}{Meters to buffer the valley floor by, where doing so
yields an inner gorge.}

\item{valley_max_hole, hollow_max_hole, gorge_max_hole}{Fill holes smaller
than this many square meters, per stage.}

\item{valley_min_patch, gorge_min_patch}{Remove patches smaller than this
many square meters, per stage.}

\item{hollow_gradient}{Named vector: PRIMARY, SECONDARY, DIF1, DIF2 and
MIN POLY GRAD gradient thresholds for hollows.}

\item{hollow_tangential}{Named vector: PRIMARY, SECONDARY and MIN POLY TAN
tangential curvature thresholds for hollows.}

\item{hollow_profile}{Named vector: PRIMARY, SECONDARY and MIN POLY PROF
profile curvature thresholds for hollows.}

\item{grad_proportion}{Named vector GRAD and PROP: a polygon is kept when at
least PROP of it exceeds gradient GRAD.}

\item{tan_proportion}{Named vector TAN and PROP, as above for tangential
curvature.}

\item{fill_hollow_embayments}{If TRUE, write the FILL HOLLOW EMBAYMENTS flag.}

\item{hollow_area_threshold}{Minimum maximum-contributing-area within a
hollow, in square meters.}

\item{hollow_min_patch}{Named vector INITIAL and FINAL: minimum patch size in
square meters before and after hollow processing.}

\item{gorge_gradient}{Named vector: PRIMARY, SECONDARY, DIF1 and DIF2
gradient thresholds for inner gorges.}

\item{slope_thresholds}{Named vector SLOPE and STEEP: upslope-gradient
thresholds classifying the remaining hillslope into "other", non-steep
and steep terrain.}

\item{curve_thresholds}{Named vector CONVERGENT and DIVERGENT: tangential
curvature thresholds splitting hillslope terrain into convergent,
divergent and planar classes.}

\item{edge_smoothing_iterations}{Smoothing iterations applied to all output
polygon edges.}

\item{road_shapefile}{Input road polyline shapefile (optional).}

\item{road_buffer}{Buffer in meters around roads. Used only when a road
shapefile is supplied.}

\item{in_closest_node, in_dist_to_channel, in_drainage_wing, in_valley_floor, in_upgrad, in_downgrad, in_grad, in_tangential, in_plan, in_prof}{Optional precomputed input rasters. Each pairs with the matching \code{out_}
argument from a previous run. RIL only skips recomputing elevation
derivatives when \emph{all six} of in_upgrad, in_downgrad, in_grad,
in_tangential, in_plan and in_prof are supplied; partial sets are
ignored and every derivative is recalculated.}

\item{out_closest_node, out_dist_to_channel, out_drainage_wing, out_valley_floor, out_upgrad, out_downgrad, out_grad, out_tangential, out_plan, out_prof}{Optional intermediate rasters to write out for reuse.}

\item{out_nodes}{Optional output node point shapefile carrying the
attributes.}

\item{out_zero_order}{Optional output zero-order basin raster.}

\item{attribute_list}{List of \code{\link[=attribute_spec]{attribute_spec()}} objects to compute for each
node. Defaults to \code{\link[=ril_default_attributes]{ril_default_attributes()}}.}

\item{use_ltd}{If FALSE, write the USE STANDARD D8 flag so channel-node
drainage wings are built with standard D8 flow paths rather than D8-LTD.}

\item{debug}{If TRUE, write the DEBUG flag. RIL then writes extra
diagnostic rasters (upgrad, downgrad, node ID) to hardcoded paths under
\verb{c:\\\\temp}, which must already exist.}

\item{overwrite}{If TRUE, allow overwriting an existing input file.}
}
\value{
The input file path, invisibly.
}
\description{
RIL delineates a channel network on a DEM and classifies the terrain around
it, producing a raster of riparian, inner-gorge and hollow landform classes
together with an optional node point shapefile carrying per-node attributes.
}
\details{
The work happens in four stages, and the parameters below are grouped to
match:
\enumerate{
\item Channel initiation: where the network begins, set by the AS2, plan
curvature and gradient thresholds, and where it becomes fluvial rather
than colluvial.
\item Valley floor delineation, by channel depth.
\item Hollow delineation, by gradient and by tangential and profile
curvature, each with a primary and a secondary threshold.
\item Inner gorge delineation, by gradient.
}
Stages 2 to 4 each end with a hole-filling and small-patch-removal pass, so
each has its own maximum hole size and minimum patch size.

Every parameter defaults to the value used in the Post Mortem reference run,
so a minimal call needs only \code{dem}, \code{scratch_dir} and \code{out_RIL}.

Grouped thresholds are given as named numeric vectors whose names are
written verbatim, so \code{c(PRIMARY = 0.3, SECONDARY = 0.25)} becomes
\verb{PRIMARY = 0.3, SECONDARY = 0.25}.
}
