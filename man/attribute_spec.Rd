% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_file_utilities.R
\name{attribute_spec}
\alias{attribute_spec}
\alias{ril_attribute}
\title{Describe one entry in an attribute list}
\usage{
attribute_spec(
  name,
  ...,
  output_field = NULL,
  file = NOFILE,
  units = NULL,
  type = NULL,
  field_length = NULL,
  replace = FALSE,
  terms = list()
)

ril_attribute(
  name,
  ...,
  output_field = NULL,
  file = NOFILE,
  units = NULL,
  type = NULL,
  field_length = NULL,
  replace = FALSE,
  terms = list()
)
}
\arguments{
\item{name}{Attribute name exactly as the program knows it, e.g. "NODE ID",
"CONTRIBUTING AREA", "MEAN ANNUAL PRECIP".}

\item{...}{Any further arguments the attribute takes, which vary by program
and by attribute. Named arguments are written as \code{NAME = value}; unnamed
arguments are written as bare flags. netrace's gradient attribute, for
instance, is \verb{attribute_spec("GRADIENT", \code{`CHANNEL WIDTHS`} = 20, output_field = "GRAD20CW")}. Because these come before the named formals
below, those formals must be given by their full names.}

\item{output_field}{Field name to write into the output node shapefile.
Omit to let the program choose.}

\item{file}{Raster to sample the attribute from, for attributes read off a
grid such as mean annual precipitation.}

\item{units}{Units of that raster, e.g. "mm".}

\item{type}{Storage type in the output shapefile, e.g. "I4".}

\item{field_length}{Field width in the output shapefile, written as the
LENGTH argument.}

\item{replace}{If TRUE, overwrite an existing field of the same name.}

\item{terms}{List of \code{\link[=equation_term]{equation_term()}} objects. Supplying any makes this an
equation attribute.}
}
\value{
An attribute object for \code{\link[=write_attribute_list]{write_attribute_list()}}.
}
\description{
Becomes one line inside the ATTRIBUTE LIST block. Entries with an equation
are followed by their TERM lines and an END EQUATION line; the EQUATION
flag is added automatically whenever \code{terms} is non-empty, so it cannot be
left off by accident.
}
