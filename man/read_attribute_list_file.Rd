% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_file_utilities.R
\name{read_attribute_list_file}
\alias{read_attribute_list_file}
\title{Read an ATTRIBUTE LIST block straight from a file path}
\usage{
read_attribute_list_file(
  path,
  start_keyword = "ATTRIBUTE LIST",
  end_pattern = "^END( ATTRIBUTE)? LIST$"
)
}
\arguments{
\item{path}{Path to a text file containing an \verb{ATTRIBUTE LIST:} block.}

\item{start_keyword, end_pattern}{Passed through to \code{\link[=get_attribute_list]{get_attribute_list()}}.}
}
\value{
A list of \code{\link[=attribute_spec]{attribute_spec()}} objects, empty if the file has no
attribute list.
}
\description{
A thin wrapper around \code{\link[=get_input_file]{get_input_file()}} + \code{\link[=get_attribute_list]{get_attribute_list()}} for
callers that only have a path, not an already-loaded input-file tibble --
e.g. a driver script that lets its own parameter file point at a \emph{separate}
text file holding an arbitrary, hand-edited \verb{ATTRIBUTE LIST:} / \verb{END LIST:}
block, so the attribute set to compute isn't hardcoded to
\code{\link[=ril_default_attributes]{ril_default_attributes()}} or any other fixed R-side list.
}
\details{
The file need not be a complete RIL/netrace input file: anything outside
the \verb{ATTRIBUTE LIST:} ... \verb{END LIST:} block is ignored, so a file holding
nothing but that block works fine.
}
\seealso{
\code{\link[=get_attribute_list]{get_attribute_list()}}, \code{\link[=RIL_input]{RIL_input()}}
}
